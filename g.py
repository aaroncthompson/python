allies = []
enemies = []

# array or list with keys etc to organize relationship points with different characters / factions?
# if relationship[dwarf] < -1000, elsif -500, elsif -250, elsif 0, elsif 250, elsif 500, elsif 1000, else
# if relationship[elf] < -1000, elsif -500, elsif -250, elsif 0, elsif 250, elsif 500, elsif 1000, else

if 'dwarf' in allies:
  # passive buff
  # dwarf discount
  # dwarf appearance in final battle
  # negative effect on relationship with elf, maybe a flat -100 or more
  
if 'elf' in allies:
  # passive buff
  # elf discount
  # elf appearance in final battle
  # negative effect on relationship with dwarf
  
if 'dwarf' in enemies:
  # positive effect on relationship with elf
  
if 'elf' in enemies:
  # positive effect on relationship with dwarf

def doathing(person, browniepts): # for both positive and negative interactions
  #relationship[person] += browniepts
